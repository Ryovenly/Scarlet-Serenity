<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.akane.scarletserenity">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".controller.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".controller.CharacterActivity">
        </activity>

        <activity android:name=".controller.CreateCharacterActivity">
        </activity>

        <activity android:name=".controller.QuizActivity">
        </activity>

        <activity android:name=".controller.SynopsisActivity">
        </activity>

        <activity android:name=".controller.MainCharacterActivity">
        </activity>

        <activity android:name=".controller.ChatActivity">
        </activity>

        <activity android:name=".controller.TchatActivity">
        </activity>

        <activity android:name=".controller.LobbyActivity">
        </activity>


        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>